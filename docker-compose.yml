version: '3'
services:
  web:
    build: .
    command: bundle exec rails server -b 0.0.0.0
    ports:
      - "3000:3000"
    volumes:
      - ./tmp/appmap:/app/tmp/appmap

  mongodb:
    image: mongo:latest
    ports:
      - "27017:27017"
    volumes:
      - ./tmp/mongodb:/data/db